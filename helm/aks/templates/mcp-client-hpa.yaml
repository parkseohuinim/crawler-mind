{{- if and .Values.mcpClient.enabled .Values.mcpClient.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "crawler-mind.fullname" . }}-mcp-client
  labels:
    {{- include "crawler-mind.mcpClient.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "crawler-mind.fullname" . }}-mcp-client
  minReplicas: {{ .Values.mcpClient.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.mcpClient.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.mcpClient.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.mcpClient.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.mcpClient.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.mcpClient.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
